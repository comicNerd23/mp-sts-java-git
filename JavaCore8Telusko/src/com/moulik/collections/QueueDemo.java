package com.moulik.collections;

/**
 * Queue declares behavior of a queue, ofter FIFO
 * 
 * 		E		element()		Doesn't remove; NoSuchElementException if queue empty
 * 		E		peek()			Doesn't remove; Returns null if queue empty
 * 		E		poll()			Removes element; Returns null if queue empty
 * 		E		remove()		Removes element; NoSuchElementException if queue empty
 * 		
 * 		boolean	offer(E obj)	trued to add object to the queue
 *
 *
 */
public class QueueDemo {

}
