package com.moulik.collections;

/**
 * Navigable Set declares behaviour of a collection that supports retrieval of elements based on the closest match
 * to a given value or values.
 * 
 * 		2 + 4 + 3 + 2 = 11
 * 				E						ceiling(E obj)
 * 				E						floor(E obj)
 * 
 * 				E						higher(E obj)
 * 				E						lower(E obj)
 * 				E						pollFirst()
 * 				E						pollLast()
 *
 *				NavigableSet<E>			headSet(E upperBound, boolean included)
 *				NavigableSet<E>			subSet(E lowerbound, boolean lowIncluded, E upperbound, boolean highIncluded)
 *				NavigableSet<E>			tailSet(E lowerBound, boolean included)
 *				NavigableSet<E>			descendingSet()
 *				Iterator<E>				descendingIterator()
 *
 */
public class NavigableSetDemo {

	public static void main(String[] args) {

	}

}
